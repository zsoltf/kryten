#!/usr/bin/env ruby

require "bundler/setup"
require "kryten"
require "pry"
require './test/test_helper'

include Kryten

class Work
  include ThreadedTask
end

w1 = Tester.new('work1')
w2 = Tester.new('work2')
w3 = Tester.new('work3')
w3.workers do
  [Tester.new('w3sub1'), Tester.new('w3sub2')]
end

visor = Supervisor.start_workers([w1,w2,w3])

binding.pry
